Installation Instructions:
to do?

Usage:
To be able to use the web application you must register an account with a valid email address and phone number.
The phone number must start with a country code, e.g. +44/+1, followed by the rest of the number. 

Once a user has registered their account a menu will show three options:

1.Send New Message:
Allows a user to send inputs (below) to the circuit board:

Temperature(-150 to 150°C)
Fan Direction(Forward/Reverse)
Last Digit(Interger 0-9999)
Switch 1(On/Off)
Switch 2(On/Off)
Switch 3(On/Off)
Switch 4(On/Off)

2.Download Messagep
Pulls messages from the M2M server and saves them in the web application database

3.View Messages
Shows all the stored messages sent(messages have to be downloaded first)

4.Logout
Ends user session